export const metadata = {
  title: "Sign In — PlayDeck",
  description: "Sign in with a passkey from the decks screen.",
}

/** Passkey auth UI is rendered by `PresenterPasskeyAuth` in `PlaydeckJazzProvider`. */
export default function SignInPage() {
  return null
}
