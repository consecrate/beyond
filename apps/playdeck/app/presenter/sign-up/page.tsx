export const metadata = {
  title: "Sign Up — PlayDeck",
  description: "Create an account with a passkey from the decks screen.",
}

/** Passkey auth UI is rendered by `PresenterPasskeyAuth` in `PlaydeckJazzProvider`. */
export default function SignUpPage() {
  return null
}
