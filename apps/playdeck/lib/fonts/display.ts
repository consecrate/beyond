import { Newsreader } from "next/font/google"

/** Display serif used for the PlayDeck wordmark and primary page titles. */
export const fontDisplay = Newsreader({
  subsets: ["latin"],
  weight: "600",
})
