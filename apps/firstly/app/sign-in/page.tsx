export const metadata = {
  title: "Sign In — Firstly",
  description: "Sign in with a passkey from the sessions screen.",
}

/** Passkey auth UI is rendered by `FirstlyPasskeyAuth` in `FirstlyJazzProvider`. */
export default function SignInPage() {
  return null
}
