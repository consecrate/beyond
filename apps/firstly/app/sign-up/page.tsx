export const metadata = {
  title: "Sign Up — Firstly",
  description: "Create an account with a passkey from the sessions screen.",
}

/** Passkey auth UI is rendered by `FirstlyPasskeyAuth` in `FirstlyJazzProvider`. */
export default function SignUpPage() {
  return null
}
